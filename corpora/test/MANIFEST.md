# Manifest — test corpus

Assign each test file to a module by hand. Run
`studykb ingest --corpus test --dry-run` first; it lists anything unassigned.

Columns: `path | module | type | enabled`.

| path | module | type | enabled |
|---|---|---|---|
